require('dotenv').config(); const mongoose = require('mongoose'); const User = require('./models/User'); async function run() { try { await mongoose.connect(process.env.MONGO_URI); await User.deleteMany({email: 'test123@buildtrack.com'}); const u = new User({ name: 'Test Admin', email: 'test123@buildtrack.com', password: 'password123', role: 'Admin', isActive: true }); await u.save(); console.log('? Created: test123@buildtrack.com / password123'); } catch(e) { console.error(e); } finally { process.exit(); } } run();
